import { FollowingDirective } from './following.directive';

describe('FollowingDirective', () => {
  it('should create an instance', () => {
    const directive = new FollowingDirective();
    expect(directive).toBeTruthy();
  });
});
